/*package com.example.demo;
import org.junit.jupiter.api.Test;

import com.example.demo.domain.Cow;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class Cowtest {

    @Test
    public void testCowConstructorAndGetters() {
        // Create a cow
        Cow cow = new Cow(1, "Bessie", "Holstein", "123 Farm Road");

        // Test getters
        assertEquals(1, cow.getId());
        assertEquals("Bessie", cow.getName());
        assertEquals("Holstein", cow.getBreed());
        assertEquals("123 Farm Road", cow.getaddress());
    }

    @Test
    public void testCowSetters() {
        // Create a cow
        Cow cow = new Cow();

        // Set properties
        cow.setId(1);
        cow.setName("Bessie");
        cow.setBreed("Holstein");
        cow.setAddress("123 Farm Road");

        // Test getters after setting properties
        assertEquals(1, cow.getId());
        assertEquals("Bessie", cow.getName());
        assertEquals("Holstein", cow.getBreed());
        assertEquals("123 Farm Road", cow.getaddress());
    }

    @Test
    public void testCowToString() {
        // Create a cow
        Cow cow = new Cow(1, "Bessie", "Holstein", "123 Farm Road");

        // Test toString method
        String expectedToString = "Cow{id=1, name='Bessie', breed='Holstein', address='123 Farm Road'}";
        assertEquals(expectedToString, cow.toString());
    }
}
*/